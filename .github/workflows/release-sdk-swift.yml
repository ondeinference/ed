name: Release SDK Swift

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"
  workflow_dispatch:
    inputs:
      publish:
        description: "Publish Ed Swift release assets from the selected ref"
        type: boolean
        default: false
      release_tag:
        description: "Existing semver tag to publish on manual runs"
        type: string
        required: false

permissions:
  contents: write

env:
  # Ed depends on onde by path, so a checkout and its sibling engine move
  # together and CI has to reproduce that layout. Both repos are public, so no
  # token is needed. Point this at `development` while Ed relies on an onde API
  # that has not been released yet, and back at `main` once it has.
  ONDE_REF: main

jobs:
  preflight:
    # macOS rather than Linux: the release artifact is an XCFramework, the whole
    # toolchain is Apple, and building onde on Linux means building mistral.rs
    # plus a GTK/WebKit stack for the Tauri crate for no coverage anyone ships.
    runs-on: macos-26
    defaults:
      run:
        working-directory: ed
    steps:
      - uses: actions/checkout@v6
        with:
          path: ed
      - uses: actions/checkout@v6
        with:
          repository: ondeinference/onde
          ref: ${{ env.ONDE_REF }}
          path: onde
      - name: Read Rust toolchain
        run: |
          rust_toolchain="$(sed -n 's/^channel = "\(.*\)"/\1/p' rust-toolchain.toml | head -n 1)"
          test -n "$rust_toolchain"
          echo "RUST_TOOLCHAIN=$rust_toolchain" >> "$GITHUB_ENV"
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ env.RUST_TOOLCHAIN }}
          # The action installs a minimal profile, so the pinned toolchain has
          # neither of these unless they are asked for by name.
          components: rustfmt, clippy
      - name: Validate tag matches workspace version
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          version="$(cargo metadata --no-deps --format-version 1 | python3 -c 'import json,sys; print(next(p["version"] for p in json.load(sys.stdin)["packages"] if p["name"] == "ed-agent"))')"
          test "${GITHUB_REF_NAME#v}" = "$version"
      - run: cargo fmt --all -- --check
      - run: cargo test --locked --workspace
      - run: cargo clippy --locked --workspace --all-targets -- -D warnings
      # Not --locked: packaging swaps the onde path dependency for the registry
      # one, which is a lockfile change by definition. This step is also the
      # gate that catches publishing Ed before the onde version it requires.
      - run: cargo publish --dry-run -p ed-agent

  build-and-release:
    needs: preflight
    runs-on: macos-26
    defaults:
      run:
        working-directory: ed
    steps:
      - uses: actions/checkout@v6
        with:
          path: ed
      - uses: actions/checkout@v6
        with:
          repository: ondeinference/onde
          ref: ${{ env.ONDE_REF }}
          path: onde
      - name: Read Rust toolchain
        run: |
          rust_toolchain="$(sed -n 's/^channel = "\(.*\)"/\1/p' rust-toolchain.toml | head -n 1)"
          test -n "$rust_toolchain"
          echo "RUST_TOOLCHAIN=$rust_toolchain" >> "$GITHUB_ENV"
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ env.RUST_TOOLCHAIN }}
          targets: aarch64-apple-ios,aarch64-apple-ios-sim,aarch64-apple-darwin
      - uses: dtolnay/rust-toolchain@nightly
        with:
          components: rust-src
      - run: .github/scripts/build-swift-xcframework.sh
      - id: meta
        run: |
          echo "version=$(cat dist/swift/version.txt)" >> "$GITHUB_OUTPUT"
          echo "checksum=$(cat dist/swift/EdFramework.checksum.txt)" >> "$GITHUB_OUTPUT"
      - id: release
        env:
          INPUT_RELEASE_TAG: ${{ inputs.release_tag }}
        run: |
          if [ "${GITHUB_REF_TYPE}" = "tag" ]; then
            release_tag="${GITHUB_REF_NAME}"
          else
            release_tag="${INPUT_RELEASE_TAG}"
          fi
          echo "release_tag=$release_tag" >> "$GITHUB_OUTPUT"
      - name: Validate release tag
        if: startsWith(github.ref, 'refs/tags/') || inputs.publish
        env:
          VERSION: ${{ steps.meta.outputs.version }}
          RELEASE_TAG: ${{ steps.release.outputs.release_tag }}
        run: test -n "$RELEASE_TAG" && test "$RELEASE_TAG" = "$VERSION"
      - name: Upload build artifact
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v7
        with:
          name: ed-swift-xcframework
          path: |
            ed/dist/swift/EdFramework.xcframework.zip
            ed/dist/swift/EdFramework.checksum.txt
            ed/dist/swift/version.txt
            ed/dist/swift/Package/Sources/Ed/ed_agent_ffi.swift
      - name: Create GitHub release
        if: startsWith(github.ref, 'refs/tags/') || inputs.publish
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.release.outputs.release_tag }}
          name: ${{ steps.release.outputs.release_tag }}
          generate_release_notes: true
          files: |
            ed/dist/swift/EdFramework.xcframework.zip
            ed/dist/swift/EdFramework.checksum.txt
      - name: Checkout onde-ed-swift
        if: startsWith(github.ref, 'refs/tags/') || inputs.publish
        uses: actions/checkout@v6
        with:
          repository: ondeinference/onde-ed-swift
          token: ${{ secrets.ED_SWIFT_PAT }}
          path: onde-ed-swift
      - name: Update Swift package
        if: startsWith(github.ref, 'refs/tags/') || inputs.publish
        working-directory: ${{ github.workspace }}
        env:
          CHECKSUM: ${{ steps.meta.outputs.checksum }}
          RELEASE_URL: https://github.com/${{ github.repository }}/releases/download/${{ steps.release.outputs.release_tag }}/EdFramework.xcframework.zip
        run: |
          python3 - <<'PY'
          import os, pathlib, re
          package = pathlib.Path("onde-ed-swift/Package.swift")
          source = package.read_text()
          source, urls = re.subn(r'(let releaseFrameworkURL =\s*\n\s*")([^"]+)(")', rf'\g<1>{os.environ["RELEASE_URL"]}\g<3>', source, count=1)
          source, sums = re.subn(r'(let releaseFrameworkChecksum =\s*\n\s*")([^"]+)(")', rf'\g<1>{os.environ["CHECKSUM"]}\g<3>', source, count=1)
          if (urls, sums) != (1, 1):
              raise RuntimeError("Package.swift release constants were not found")
          package.write_text(source)
          PY
          cp ed/dist/swift/Package/Sources/Ed/ed_agent_ffi.swift onde-ed-swift/Sources/Ed/ed_agent_ffi.swift
      - name: Commit and tag Swift package
        if: startsWith(github.ref, 'refs/tags/') || inputs.publish
        working-directory: ${{ github.workspace }}/onde-ed-swift
        env:
          VERSION: ${{ steps.release.outputs.release_tag }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Package.swift Sources/Ed/ed_agent_ffi.swift
          git diff --cached --quiet || git commit -m "Release $VERSION"
          git push origin HEAD:main
          if git ls-remote --tags origin "refs/tags/$VERSION" | grep -q "refs/tags/$VERSION"; then
            echo "::warning::Tag $VERSION already exists; leaving it unchanged."
          else
            git tag -a "$VERSION" -m "Release $VERSION"
            git push origin "$VERSION"
          fi
